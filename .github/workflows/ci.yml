name: CI

on:
  push:
  pull_request:

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: recall-server
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest pytest-asyncio httpx ruff black mypy bandit
      - name: Lint (ruff)
        run: ruff check recall
      - name: Format check (black)
        run: black --check recall tests
      - name: Type check (mypy)
        run: mypy recall || true
      - name: Security scan (bandit)
        run: bandit -r recall -q -ll
      - name: Tests
        run: pytest -q
      - name: Docker build
        working-directory: .
        run: docker build -f docker/Dockerfile -t recall-api:test .
