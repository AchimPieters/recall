# Agent Enterprise Layout (Transitional)

This directory is the target root for enterprise player/agent development.

- Source currently mirrors `recall-player/` as a migration baseline.
- New modules and hardening changes should land in `agent/` first.
- Legacy path will be removed after runtime and packaging switch-over is completed.
